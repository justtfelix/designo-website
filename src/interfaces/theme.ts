export interface Theme {
  colors: {
    primary: {
      peach: string;
      black: string;
      white: string;
    };
    secondary: {
      lightPeach: string;
      lightGrey: string;
      darkGrey: string;
    };
  };
}
